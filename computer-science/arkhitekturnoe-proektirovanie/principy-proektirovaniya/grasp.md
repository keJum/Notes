---
description: General Responsibility Assignment Software Patterns
---

# 🏛️ 📐🎢 GRASP

GRASP — это хорошо документированные, стандартизированные и проверенные временем принципы объектно-ориентированного анализа.

## Information Expert \( Информационный эксперт \)

Объект владеющей всей нужной информацией полностью для какой-то операции или функционала, то значит, что этот объект будет выполнять всё сам, либо получать делегировать выполнение этой операции от других.

## Creator \( Создатель \) 

Суть ответственности такого объекта в том, что он создает другие объекты.

Но есть ряд моментов, которые должны выполняться, когда мы наделяем объект ответственностью создатель:

1. Создатель содержит или агрегирует создаваемые объекты;
2. Создатель использует создаваемые объекты ;
3. Создатель знает, как проинициализировать создаваемый объект ;
4. Создатель записывает создаваемые объекты.

## Controller \( Конроллер \)

Объект-прослойка между UI-логикой и предметной \(бизнес\) логикой приложения.

## Low Coupling \( Слабая связанность \) 

Объекты в приложении сильно связанны, то любой их изменение приводит к изменениям во всех связанных объектах.

 Программируйте на основе абстракций \(интерфейс, абстрактный класс и т.п.\), а не реализаций.

## High Cohesion \( Сильная закреплённость \) 

Принцип твердит, что класс должен стараться выполнять как можно меньше не специфичных для него задач, и иметь вполне определенную область применения.

## Pure Fabrication \( Чистая выдумка \)

Здесь суть в выдуманном объекте. Этот объект реализует 

## Indirection \( Посредник \) 

Слабое зацепление между элементами системы \(и возможность повторного использования\) обеспечивается назначением промежуточного объекта их **посредником**.

## Protected Variations \( Сокрытие реализации \) 

Принцип говорит определить «точки изменений» и зафиксировать их в абстракции \(интерфейсе\). «Точки изменений» — не что иное, как наши объекты, которые могут меняться.

## Polymorphism \( Полиморфизм \) 

Он дает возможность трактовать однообразно разные объекты с одинаковым интерфейсом \(спецификацией\).

## Используемая литератцра 

* [http://blogerator.org/page/oop-patterny-i-principy-grasp](http://blogerator.org/page/oop-patterny-i-principy-grasp)
* [https://habr.com/ru/post/38323/](https://habr.com/ru/post/38323/)
* [https://www.youtube.com/watch?v=vmeLPcI-Lqk](https://www.youtube.com/watch?v=vmeLPcI-Lqk)











