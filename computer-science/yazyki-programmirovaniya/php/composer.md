---
description: Пакетный менеджер
---

# 👨🏻‍💻 🐘 📦 Composer

## Подключение компонентов 

Компоненты находиться на сайте [Packagist](https://packagist.org). Пакеты именуются по &lt;имя\_производителя&gt;\&lt;имя\_пакета&gt; \( `monolog/monolog` \). 

```bash
$ composer require [--global]  monolog/monolog
```

Ключ `--global`    подключает пакеты глобально.

Далее будет записано в файл `composer.json` следующее:

```javascript
{
    "require":{
        "monolog/monolog": "1.17.*"
    }
}
```

Для установки компонента нужно выполнить:

```bash
$ composer install
```

После установки, будет создан файл  `composer.lock` в котором находятся зависимости компонентов, а также папка  `vendor`  и в этой папке будут находится файл загрузчик всех компонентов  `autoload.php`  который будет загружать все компоненты из директорий компонентов в этой папке.

```php
require_once(__DIR__.'/vendor/auroload.php');

//Теперь можно использовать компонента
$log = new Monolog\Logger('name');
$handler = new Monolog\Hanler\StreamHadler('app.log', Monolog\Logger::WARNING);
$log->pushHandler($handler);
$log->addWorning('Предупреждение');
```

## Автозагрузка классов 

Создание проекта через composer:

```bash
$ cmposer init
```

Далее будет создан файл `composer.json` . 

Для подключения всех классов нужно в секции `autoload`  указать подраздел `psr-4` далее все файлы и классы будут подключены по стандарту PSR-4, например `Sevices\\:services`. 

В случае если классы не сосуществуют  то нужно подписать подраздел `classmap` , где есть массив указывающий где искать класс, без указания имени класса, например `service/myserv/Ksl.php`.

Подраздел `files` в секции `autload` указывает какие файлы нужно загрузить вначале, например `config.php` .

```javascript
"autoload": {
    "classmap": [
        "services/myserv/Ksl.php"
    ],
    "psr-4": {
        "Services\\": "services",
    },
    "files": [
        "config.php"
    ]
}
```

Если папка vendor с данными файлами уже была, а вам нужно изменить данные для подключения своих классов, то нужно выполнить команду:  


```text
composer dump-autoload -o
```

## Используемая литература 

* [https://klisl.com/composer\_autoload.html](https://klisl.com/composer_autoload.html)
* [https://m.habr.com/ru/post/145946/](https://m.habr.com/ru/post/145946/)



